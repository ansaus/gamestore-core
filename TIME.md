# TIME — фактическое время

Считано по коммитам, время UTC. Интервал между коммитами — это wall clock:
внутри него и написание кода, и живые прогоны сценариев (они идут минутами:
таймауты по 2 секунды, тики планировщика раз в минуту, сид на 500 000 ключей —
46 секунд), и чтение задания, и перерывы. Отделить «чистое кодирование» я не могу
и выдумывать не буду.

| Этап | Коммит | Завершён | Интервал | Что внутри |
|---|---|---|---|---|
| 0. Bootstrap | `85d6086` | 02.09, 11:46 | — | Laravel 12, Docker Compose, каркас, план работ |
| 1. Ядро API | `665bd3a` | 02.09, 12:09 | 0:23 | заказы, вебхук, выдача одним поставщиком, вся схема БД |
| 2. Exactly-once | `94e883c` | 03.09, 09:12 | 0:51 | ревизия репозитория, подхват сирот, `race.php` на `curl_multi` |
| 3. Устойчивость | `ed9a3be` | 03.09, 12:24 | 3:12 | ретраи тем же `request_id`, фолбэк, сценарии 4–5 |
| 4. Сверка | `53806db` | 03.09, 17:12 | 4:48 | отчёт, watchdog, админ-ручки, структурные логи |
| 5. Витрина | `8694a49` | 03.09, 18:52 | 1:40 | каталог, keyset, кеш, нагрузочный сид, bench |

**Итого по дню 2:** с 08:21 (отметка времени первого `make test`) до 18:52 —
**10 часов 31 минута** wall clock на этапы 2–5.

**По дню 1 честной цифры нет.** Bootstrap — первый коммит в репозитории, до него
измерять нечего, а этап 1 лёг через 23 минуты после него: очевидно, что каркас и
ядро делались одним заходом, начало которого git не зафиксировал. По объёму
(3 273 строки, 67 файлов, вся схема БД сразу) это заметно больше
получаса, но сколько именно — не знаю.

## Где ушло время

**Этап 4 (4:48) — самый дорогой, и не из-за объёма.** Отчёт сверки это восемь
SQL-запросов, каждый из которых надо было продумать: что считать находкой, а что
нормальной асинхронностью. Плюс сюда попал `StructuredFormatter` — переделка
формата логов, которая по плану относится к этому же этапу.

**Этап 3 (3:12) — время съели живые прогоны.** Сценарий с таймаутом принципиально
не ускоряется: клиент честно ждёт 2 секунды на попытку. Плюс здесь всплыли две
находки из NOTES (`SAVEPOINT` на конфликте кода и невозможность повторно запустить
заказ, зависший в `delivering`), и каждая — это отладка на реальном стеке, а не
правка в редакторе.

**Этап 2 (0:51) прошёл быстрее остальных**, хотя в этот интервал попала ещё и
ревизия репозитория с нуля. Помогло то, что механика была заложена этапом 1:
`payment_events` с `applied_at` и частичным индексом существовали с самого начала,
оставалось написать подхват и скрипт гонки.

**Этап 5 (1:40) — почти всё время в сиде и замерах.** Сам код витрины небольшой;
генерация 500 000 ключей, `ANALYZE` и прогоны `EXPLAIN` заняли больше, чем
написание запроса.

## Готовность

Definition of Done закрыт полностью: 98 тестов зелёные, все состязательные
сценарии из [docs/TASK.md](docs/TASK.md) воспроизводятся одной `make`-целью и
печатают вердикт, `reconcile` отдаёт `healthy: true` после всех прогонов, ни одного
HTTP-вызова внутри транзакции, каждый инвариант закрыт ограничением БД.
